/**
 * Sets f_ functionality to a plain object.
 * @module set/object
 */

/**
 * Binds f_ object functionality to the 1st argument. The 2nd options
 * argument allows configuration of the f_ functionality. Exported function.
 * @public
 * @param {object} object - Object to set f_ object data to
 * @param {object} options - `f_.set.object` user options
 * @return {object} object - Modified object, with f_ instance data
 */
function setObject(object, options) {

};

module.exports = setObject;